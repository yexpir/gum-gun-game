using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class Saltar : MonoBehaviour
{
    /*private float _gravity = -9.8f;
    private Vector3 jumpMove;
    void Update()
    {
        Vector3 move = transform.forward * _vMove + transform.right * _hMove;
        jumpMove.y += _gravity * Time.deltaTime;
        _characterController.Move((_move + _jumpMove) * Time.deltaTime);
    }*/
}
